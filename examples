HARIS Examples

+- comments -+

BOOL #t, #f
INT 5
LIST ^INT $ INT^
{BOOL, INT} +- tuple examples -+


[skiadas function_name [inputs] 
  [body]
]

let (e1) in (e2)

group (e1) in
$ (e2) >> (e3)


PARSER
(*
    | CURLY_L expr_tuple CURLY_R      { TupS ($2)}
  *)

(*
expr_tuple:
  | first COMMA second                        { $1 :: $3 } 
  | head COMMA expr_tuple                     { $1 :: $3 }
*)